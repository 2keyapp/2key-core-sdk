# Platform tenants

Canonical tenant registry and CatalogSeed packages moved to
**[`2key-browser-sdk`](https://github.com/2keyapp/2key-browser-sdk)** (`docs/TENANTS.md`, `catalogs/`).

This private repo keeps Rust DP clients/CLIs only. Wire catalogs from npm/`@2key/catalog-*` at deploy time.
