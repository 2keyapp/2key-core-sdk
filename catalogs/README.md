# Tenant catalogs moved

Canonical CatalogSeed packages live in **[`2key-browser-sdk`](https://github.com/2keyapp/2key-browser-sdk)** under `catalogs/`.

```ts
import { CATALOG_SEED, SERVICE_ID } from "@2key/catalog-idr";
```

See `2key-browser-sdk/docs/TENANTS.md`.
